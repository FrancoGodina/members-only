extends layout

block content

    if (!user.member)
        div(class="d-flex flex-column p-2 justify-content-center align-items-center mt-5")
            form(action="/member" method="POST")
                h2= title
                div
                    label(for="passcode" class="form-label") Enter passcode to gain membership status!
                    input(name="passcode" type="password" required="true" class="form-control")
                    if (passcodeError)
                        p #{passcodeError}
                br
                button(type="submit" class="btn btn-primary") Submit passcode

            if (errors)
                p #{errors}
    else
        div(class="d-flex flex-column p-2 justify-content-center align-items-center mt-5")
            h1 Amazing! You are a member!